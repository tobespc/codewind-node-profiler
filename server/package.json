{
	"name": "codewind-node-profiler-server",
	"description": "Codewind Language Server for Node.js profiling",
	"version": "0.9.0",
	"author": "",
	"license": "",
	"engines": {
		"node": "*"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/eclipse/codewind-node-profiler"
	},
	"dependencies": {
		"@types/node": "^8.10.39",
		"fs-extra": "^7.0.1",
		"typescript": "^3.2.2",
		"vscode-languageserver": "^4.1.3"
	},
	"scripts": {
		"prepare": "yarn run clean && yarn run build",
		"clean": "rm -rf lib",
		"build": "tsc",
		"watch": "tsc -w"
	}
}
